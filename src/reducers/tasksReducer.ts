export const TasksReducer = (state: any, action: any) => {
    switch (action.type) {
        case 'XXX': {
            return state
        }
        default:
            return state
    }
}

export const removeTaskAC = () => {
    return {

    }
}